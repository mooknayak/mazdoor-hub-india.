# Node / npm (used only for Firebase CLI, not the site itself)
node_modules/
npm-debug.log*
package-lock.json

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebaserc

# Environment / secrets (if you later move config to a .env file)
.env
.env.local

# OS / editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
